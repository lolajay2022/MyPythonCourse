import math

pi_estimate = 22/7

print('pi_estimate', pi_estimate)

print('math.trunc',math.trunc(pi_estimate))
print('floor', math.floor(pi_estimate))
print('ceil', math.ceil(pi_estimate))

print('round',round(pi_estimate))
